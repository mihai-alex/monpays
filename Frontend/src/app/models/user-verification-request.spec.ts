import { UserVerificationRequest } from './user-verification-request';

describe('UserVerificationRequest', () => {
  it('should create an instance', () => {
    expect(new UserVerificationRequest()).toBeTruthy();
  });
});
