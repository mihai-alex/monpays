import { EOperationType } from '../constants/enums/e-operation-type';

export class Operation {
  operation!: EOperationType;
  groupName!: string;
}
// TODO: initialize some of the properties if needed
