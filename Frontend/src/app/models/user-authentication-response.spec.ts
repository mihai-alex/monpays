import { UserAuthenticationResponse } from './user-authentication-response';

describe('UserAuthenticationResponse', () => {
  it('should create an instance', () => {
    expect(new UserAuthenticationResponse()).toBeTruthy();
  });
});
