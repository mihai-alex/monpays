import { ProfileHistoryEntry } from './profile-history-entry';

describe('ProfileHistoryEntry', () => {
  it('should create an instance', () => {
    expect(new ProfileHistoryEntry()).toBeTruthy();
  });
});
