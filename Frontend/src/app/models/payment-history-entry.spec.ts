import { PaymentHistoryEntry } from './payment-history-entry';

describe('PaymentHistoryEntry', () => {
  it('should create an instance', () => {
    expect(new PaymentHistoryEntry()).toBeTruthy();
  });
});
