import { AccountHistoryEntry } from './account-history-entry';

describe('AccountHistoryEntry', () => {
  it('should create an instance', () => {
    expect(new AccountHistoryEntry()).toBeTruthy();
  });
});
